package partials

templ URLResult(url string, errMsg string) {
	<div id="url-result">
		if errMsg != "" {
			<div id="url-result--error">{ errMsg }</div>
		}
		<div id="url-result--url" class={ templ.KV("with-error", errMsg != "") }>
			<a href={ templ.SafeURL(url) }>{ url }</a>
		</div>
	</div>
}
